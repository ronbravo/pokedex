export default {
  props: ['info'],
  data () {
    // let { items } = this.info;
    // let classNames, motif;
    //
    // if (!items) { items = []; }
    //
    // // Make sure the items are setup properly.
    // motif = 'raspberry';
    // items.forEach((item) => {
    //   if (!item.className) { item.className = 'eds-menu-item eds-menu-item'; }
    //   item.className = `${item.className}.${motif}`;
    // })

    return {
      // className: {
      //   sidebar: `eds-sidebar eds-sidebar.open eds-sidebar.${motif}`,
      //   collapse: `eds-sidebar_.eds-sidebar-toggle eds-sidebar.open_.eds-sidebar-toggle eds-sidebar-toggle eds-sidebar-toggle.${motif}`,
      // },
      // items,
    }
  }
}
